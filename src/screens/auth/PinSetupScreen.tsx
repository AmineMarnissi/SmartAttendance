import React from 'react';
import {View, Text} from 'react-native';

const PinSetupScreen = () => (
  <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
    <Text>PIN Setup Screen</Text>
  </View>
);

export default PinSetupScreen;
